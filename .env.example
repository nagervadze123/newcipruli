NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Admin panel — the only email allowed to sign in and manage products.
# Must match the auth user in Supabase and the email hard-coded in RLS.
ADMIN_EMAIL=
